A new faster version of Dgraph executor. Use like this:

```
const executeDgraph = require('dgraph-new').default

// Or in TS
// import executeDgraph from 'dgraph-new'

executeDgraph(graph, inputs) 
```